{% assign faq_content = product.metafields.custom.frequent_ask_question  | metafield_tag  | strip_html: true %}

{{ faq_content }}
{% if faq_content != blank %}
<div class="faq-section max-w-2xl mx-auto mt-12">
  {% assign faqs = faq_content | split:'<h6>' %}
  {% for faq in faqs %}
    {% if faq != blank %}
      {% assign parts = faq | split:'</h6>' %}
      {% assign question = parts[0] | strip %}
      {% assign answer = parts[1] | strip %}
      <div class="faq-item border rounded-lg mb-3 p-4 bg-white shadow-sm">
        <button type="button" class="faq-question flex justify-between items-center w-full text-left focus:outline-none">
          <span>{{ question }}</span>
          <span class="faq-toggle">+</span>
        </button>
        <div class="faq-answer mt-2 hidden text-gray-700">
          {{ answer | strip_html: false }}
        </div>
      </div>
    {% endif %}
  {% endfor %}
</div>

<style>
.faq-question {
  font-weight: 600;
  font-size: 1rem;
  cursor: pointer;
}
.faq-toggle {
  transition: transform 0.3s ease;
}
.faq-answer {
  font-size: 0.95rem;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const items = document.querySelectorAll('.faq-item');
  items.forEach(item => {
    const btn = item.querySelector('.faq-question');
    const answer = item.querySelector('.faq-answer');
    const toggle = item.querySelector('.faq-toggle');

    btn.addEventListener('click', () => {
      const isOpen = !answer.classList.contains('hidden');
      if(isOpen) {
        answer.classList.add('hidden');
        toggle.textContent = '+';
      } else {
        answer.classList.remove('hidden');
        toggle.textContent = '-';
      }
    });
  });
});
</script>
{% endif %}


{% schema %}
{
  "name": "FAQ Section",
  "settings": [
    {
      "type": "text",
      "id": "faq_heading",
      "label": "Heading",
      "default": "Frequently Asked Questions"
    },
    {
      "type": "text",
      "id": "faq_subheading",
      "label": "Subheading",
      "default": "Everything you need to know about TheraFace Mask"
    }
  ],
  "presets": [
    {
      "name": "FAQ Section",
      "category": "Custom"
    }
  ]
}
{% endschema %}

